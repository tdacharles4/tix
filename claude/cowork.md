# Boleteo — Cowork Sync

Last updated by: Windows (Cowork desktop session)

---

## Project Summary

Ticket sales platform (boletería) for the Mexican market.

**Stack:** Next.js 14 App Router · Supabase (auth + DB + RLS) · Conekta (MX payments, not yet live) · Resend (email) · Vercel

**User roles:** Organizers create events and generate checkout links. Buyers purchase via those links. Door staff scan QR codes at entry.

---

## Current DB Tables

| Table | Purpose |
|---|---|
| profiles | Extends auth.users. Has role and status ('pending' default) |
| events | Organizer events. Has location_type enum, ticket phases support |
| lugares | Organizer's saved venues |
| orders | One per purchase. Links to event + buyer |
| tickets | One per ticket. holder_name, no qr_code column (HMAC signed at display time) |
| checkout_sessions | One-time links generated by organizer. Has quantity + ticket_type_config_id |
| ticket_phases | Phase system per event (Early Bird, General, VIP waves) |
| ticket_type_configs | Ticket types per phase. Has its own price_mxn and quantity |

---

## Task List

| # | Status | Task |
|---|---|---|
| 1 | ✅ Done | Get current DB schema from Supabase |
| 2 | ✅ Done | Fix middleware: profile.status check (column exists, was fine) |
| 3 | 🔴 Pending | Fix atomic ticket redemption race condition |
| 4 | 🔴 Pending | Build check-in session system (scanner auth) |
| 5 | 🔴 Pending | Enforce max 4 tickets per purchase in reserve_tickets RPC |
| 6 | 🟡 Pending | Add inventory rollback on Conekta order creation failure |
| 7 | ⏳ Blocked | Wire up Conekta payment integration (blocked on 3,4,5,6) |
| 8 | 🔴 Pending | Fix checkout: use ticket_type_configs.price_mxn not events.price_mxn |
| 9 | 🔴 Pending | Fix checkout: use session.quantity not request body quantity |
| 10 | 🟡 Pending | Implement ticket_phases validation at checkout |

---

## Bugs Documented (see teachings.txt for full detail)

**Bug #8 — Wrong price at checkout**
`ticket_type_configs` has its own `price_mxn`. Checkout always uses `events.price_mxn`.
Fix: fetch price from ticket type config when session has `ticket_type_config_id`, pass it into the `reserve_tickets` RPC as an optional unit price override.

**Bug #9 — Buyer controls quantity**
`checkout_sessions.quantity` is the organizer-locked quantity. The route uses the buyer's submitted quantity instead.
Fix: after session validation, replace `quantity` with `session.quantity` and ignore the request body value.

**Bug #3 — Redemption race condition**
Redeem endpoint does read → update in two DB round trips. Two simultaneous scans can both pass the active check.
Fix: Postgres RPC that does `UPDATE ... WHERE status = 'active' RETURNING *` atomically. If 0 rows returned → already redeemed.

---

## Architecture Decisions Logged

- QR codes are NOT stored in DB. The token is `signTicket(ticket.id)` = `ticketId.hmac16` generated at display time using HMAC-SHA256 with QR_SECRET.
- Inventory reservation uses `SELECT FOR UPDATE` in the `reserve_tickets` Postgres RPC — race-safe.
- Checkout links are single-use (`checkout_sessions.used`), time-limited (`expires_at`, default 30min), and quantity-locked.
- Check-in staff will use event-scoped session links (not full Supabase accounts) — `check_in_sessions` table to be built (Task #4).
- Max 4 tickets per purchase — to be enforced in RPC + API route (Task #5).
- Conekta is fully stubbed. Orders marked paid immediately in dev. Webhook handler exists and is idempotent.

---

## Next Up

Bugs first (#8, #9, #3), then new features (#4 check-in sessions, #5 max cap, #10 phase validation), then Conekta live (#7).
